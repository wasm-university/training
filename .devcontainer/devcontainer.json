{
  "image": "k33g/wasm-university-training:0.0.1",
  "forwardPorts": [8080,3000,5000],
  "customizations": {
    // Configure properties specific to VS Code.
    "vscode": {
      // Add the IDs of extensions you want installed when the container is created.
      "extensions": ["golang.go", "rust-lang.rust", "bungcip.better-toml", "aaron-bond.better-comments"
      ]
    }
  }
}
